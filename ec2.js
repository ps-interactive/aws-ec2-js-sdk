const fs = require('fs');
const { command, message, keyExists } = require('./helpers')

/****************
AWS Configuration
*****************/


switch (command) {
  default:
    console.error('Not a valid command!');
    break;
}
